"""Dynamic panel AR(1) Monte Carlo: pooled OLS vs first differences vs within.

The design, the estimators and every benchmark number live in SPEC.md at the
repo root; this package implements them and nothing else.
"""

__version__ = "0.1.0"
